{
  "label": "Release Notes Export Directory",
  "description": "Filepath where we should dump the release notes once generated",
  "schema": {
    "properties": {
      "environmentVariableName": "RELEASE_NOTES_EXPORT_DIR",
      "value": {
        "type": "string",
        "default": "Please insert value here"
      }
    },
    "required": ["value"]
  }
}
